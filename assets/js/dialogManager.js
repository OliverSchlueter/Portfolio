function showDialog(dialogID){
    var dialogContainer = document.getElementById(dialogID);
    dialogContainer.style.visibility = "visible";
}

function hideDialog(dialogID){
    var dialogContainer = document.getElementById(dialogID);
    dialogContainer.style.visibility = "hidden";
}